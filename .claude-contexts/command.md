@AGENTS.md
@DECISIONS.md
@docs/skills/skills.md
@docs/skills/agent-efficiency.md

<!--
Role: interactive command handler (ai-command.yml — the `/ai <cmd>` slash
trigger) and the factory manager (ai-factory-manager.yml).

Both roles can be asked to do anything across the repo, so they need a broad
context — full AGENTS + DECISIONS + skills index. ARCHITECTURE.md is left out
of the chain (still readable on demand) because most slash commands are
operational, not design-level.
-->
